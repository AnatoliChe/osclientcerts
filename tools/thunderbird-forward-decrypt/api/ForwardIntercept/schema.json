[
  {
    "namespace": "ForwardIntercept",
    "description": "Privileged interception of the Forward compose flow for embedded message/rfc822 S/MIME containers. When a user forwards such a message, the compose-type is redirected to a Reply before the compose window is created, so only a single reply window opens (no Forward-window flash) carrying the full decrypted content.",
    "functions": [
      {
        "name": "getEnabled",
        "type": "function",
        "description": "Whether the Forward->Reply interception is currently active.",
        "async": true,
        "parameters": []
      },
      {
        "name": "setEnabled",
        "type": "function",
        "description": "Enable or disable the Forward->Reply interception.",
        "async": true,
        "parameters": [
          {
            "name": "enabled",
            "type": "boolean",
            "description": "True to intercept, false to pass through untouched."
          }
        ]
      },
      {
        "name": "getComposeAttachments",
        "type": "function",
        "description": "Parent-scope read of the decrypted attachment model that Thunderbird built for a compose window. Thunderbird's reply compose carries the decrypted body + inline images, and (for an embedded message/rfc822 S/MIME container) may also carry standalone file attachments in nsIMsgCompFields.attachments — which the WebExtension messages API cannot reach. Returns an array of {name, size, contentType, isInline} for each non-envelope attachment part it can expose, or an empty array if none.",
        "async": "true",
        "parameters": [
          {
            "name": "tabId",
            "type": "number",
            "description": "The messageCompose tab id whose compose window's attachments to read."
          }
        ]
      }
    ]
  }
]