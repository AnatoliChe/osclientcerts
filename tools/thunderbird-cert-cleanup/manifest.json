{
  "manifest_version": 3,
  "name": "OS Client Certs -- cert9.db cleanup",
  "version": "0.6.4.4",
  "description": "TEST SEARCH-ONLY BUILD (v0.6.4.4) = 0.6.4.3 + full cleanup restored on app shutdown. Send path unchanged from 0.6.4.3: doCleanup(email) via ONE certDB.findCertByEmailAddress() + direct SQL DELETE of the matching cert9.db row by DER (no getCerts(), no constructX509/deleteCertificate -- 0.6.4.1 proved full enumeration breaks reading mid-session). NEW: the original v0.6.3 doCleanup() (getCerts + email filter + SQL read + constructX509/deleteCertificate) is restored verbatim as doCleanupFin() and re-bound ONLY to the app-shutdown trigger, where disturbing NSS/PSM state is harmless because the process exits right after. Purpose: verify 0.6.4.3 cleanly fixes P1 (signing) at send while keeping reading working, with the full session-clearing pass still happening on quit.",
  "browser_specific_settings": {
    "gecko": {
      "id": "cert-cleanup-experiment@osclientcerts.dev",
      "strict_min_version": "153.1.1",
      "update_url": "https://raw.githubusercontent.com/AnatoliChe/osclientcerts/tools-cert-cleanup-v0.6.1-proactive-triggers/tools/thunderbird-cert-cleanup/updates.json"
    }
  },
  "experiment_apis": {
    "certCleanup": {
      "schema": "experiments/certCleanup/schema.json",
      "parent": {
        "scopes": ["addon_parent"],
        "paths": [["certCleanup"]],
        "script": "experiments/certCleanup/implementation.js"
      }
    }
  },
  "background": {
    "scripts": ["background.js"]
  }
}
