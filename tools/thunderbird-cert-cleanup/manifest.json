{
  "manifest_version": 3,
  "name": "OS Client Certs -- cert9.db cleanup",
  "version": "0.4.4",
  "description": "EXPERIMENT BUILD, not for production: fixes reloadOwnModule() crashing (nsISimpleEnumerator isn't for-of-iterable in this scope) so the PKCS#11 module reload actually runs. Otherwise same as 0.4.3: single-shot cleanup 10 seconds after startup, no periodic timer. See tools/thunderbird-cert-cleanup/README.md and the experiment/cert-cleanup-mid-session branch.",
  "browser_specific_settings": {
    "gecko": {
      "id": "cert-cleanup-experiment@osclientcerts.dev",
      "strict_min_version": "128.0"
    }
  },
  "experiment_apis": {
    "certCleanup": {
      "schema": "experiments/certCleanup/schema.json",
      "parent": {
        "scopes": ["addon_parent"],
        "paths": [["certCleanup"]],
        "script": "experiments/certCleanup/implementation.js"
      }
    }
  },
  "background": {
    "scripts": ["background.js"]
  }
}
